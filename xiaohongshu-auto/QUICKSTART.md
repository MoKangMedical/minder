# 🚀 快速开始

## 1. 安装依赖

```bash
cd xiaohongshu-auto
npm install
```

## 2. 登录小红书

```bash
npm run login
```

按提示完成登录，Cookie会自动保存。

## 3. 测试预览

```bash
npm run preview
```

这会生成内容和图片，但不会发布。

## 4. 启动自动运营

```bash
npm start
```

系统会在每天 9:00、12:00、18:00 自动发布。

---

## 📁 文件结构

```
xiaohongshu-auto/
├── index.js              # 主入口
├── package.json          # 依赖配置
├── README.md             # 完整文档
├── config/
│   └── config.js         # 配置文件
├── src/
│   ├── scheduler.js      # 定时调度器
│   ├── ai/
│   │   └── kimi-client.js      # AI内容生成
│   ├── image/
│   │   └── image-generator.js  # 图片生成
│   ├── data/
│   │   └── minder-client.js    # 念念数据
│   └── publish/
│       ├── xhs-publisher.js    # 小红书发布
│       ├── xhs-login.js        # 登录助手
│       └── manual-publish.js   # 手动发布
├── data/
│   └── minder-data.json  # 示例数据
├── cookies/              # Cookie存储
├── screenshots/          # 图片存档
└── logs/                 # 运行日志
```

---

## ⚡ 常用命令

| 命令 | 说明 |
|------|------|
| `npm start` | 启动定时任务 |
| `npm run login` | 登录小红书 |
| `npm run preview` | 预览生成内容 |
| `npm run publish:manual` | 手动发布一条 |

---

## ⚠️ 注意事项

1. **首次使用必须先登录** - 运行 `npm run login`
2. **控制发布频率** - 建议每天不超过3条
3. **监控账号状态** - 定期检查是否被限流
4. **准备人工接管** - 自动化有风险

---

## 🐛 故障排查

**Q: 登录失败？**  
A: 确保使用正确的账号密码，或尝试扫码登录

**Q: 发布失败？**  
A: 检查 `logs/` 目录下的错误日志

**Q: Cookie失效？**  
A: 重新运行 `npm run login`

---

✨ 现在可以开始自动运营了！
